#pragma once

/*
int listen(int sockfd, int backlog);

backlog is the number of connections allowed on the incoming queue.

incomming connections are going to wait in this queue until you accept() them

*/

/*
getaddrinfo();
socket();
bind();
listen();
*/

